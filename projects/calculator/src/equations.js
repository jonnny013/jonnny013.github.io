const addition = (x, y) => {
    return x + y
}

const subtraction = (x, y) => {
    return x - y
}

const multiplacation = (x, y) => {
    return x * y
}

const division = (x, y) => {
    return x / y 
}

export default {
    addition,
    subtraction,
    multiplacation,
    division
}